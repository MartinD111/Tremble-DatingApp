---
phase: 06-brand-alignment-wave-mechanic
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/src/core/translations.dart
autonomous: true
requirements: [BRAND-03, BRAND-04]

must_haves:
  truths:
    - "Onboarding copy for onb1_body, onb2_body, onb3_title, onb3_body is rewritten in all 8 languages to brand voice"
    - "greeting_sent key is renamed to wave_sent in all 8 language blocks and the value no longer contains {name}"
    - "onb4_title and onb4_body in English are updated to brand voice"
    - "onb4_title and onb4_body in Slovenian are updated to brand voice"
    - "All 8 language blocks have wave_sent key and no greeting_sent key"
  artifacts:
    - path: "lib/src/core/translations.dart"
      provides: "Brand-aligned copy in all 8 languages"
      contains: "wave_sent"
  key_links:
    - from: "lib/src/core/translations.dart"
      to: "greeting_sent key usages"
      via: "key rename — grep lib/ for greeting_sent to confirm all consumers updated"
      pattern: "wave_sent"
---

<objective>
Rewrite onboarding copy in all 8 language blocks of translations.dart to match Tremble brand voice, and rename the greeting_sent key to wave_sent (removing the name variable — privacy improvement).

Purpose: BRAND-03 and BRAND-04 compliance — short, direct, confident copy that reflects what Tremble actually does.
Output: translations.dart with brand-voice copy in EN/SL/DE/IT/FR/HR/SR/HU, wave_sent key replacing greeting_sent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-brand-alignment-wave-mechanic/RESEARCH.md
@lib/src/core/translations.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite onboarding copy in all 8 language blocks</name>
  <files>lib/src/core/translations.dart</files>
  <read_first>
    - lib/src/core/translations.dart (read the FULL file before making any edits — you need line numbers for all 8 language blocks before touching anything)
  </read_first>
  <action>
    The translations.dart file has 8 language sections: en, sl, de, it, fr, hr, sr, hu. For each section, update the onboarding copy keys as specified below.

    **ENGLISH (en) — lines ~346–357:**
    Change these exact keys to these exact values:
    ```dart
    'onb1_title': 'IT RUNS WHILE YOU LIVE.',
    'onb1_body':
        'Tremble works in the background. When someone compatible is physically near you, you get a signal. No scrolling required.',
    'onb2_title': 'ZERO SWIPES. ONE WAVE.',
    'onb2_body':
        'No feeds. No algorithms. No endless chat rooms. Just a proximity signal and one decision: wave or move on.',
    'onb3_title': 'YOUR CITY, LIVE.',
    'onb3_body':
        'See who\'s discovering nearby in real time. The more Tremble users around you, the higher the signal.',
    'onb4_title': 'YOUR RULES.',
    'onb4_body':
        'Unmatch or block at any time. Unidirectional — no one knows you waved until it\'s mutual.',
    ```

    **SLOVENIAN (sl) — lines ~662–673:**
    ```dart
    'onb1_title': 'DELUJE, MEDTEM KO ŽIVIŠ.',
    'onb1_body':
        'Tremble deluje v ozadju. Ko je nekdo združljiv z blizu tebe, dobiš signal. Brskanja ni potrebnega.',
    'onb2_title': 'NIČ POTEZ. EN VAL.',
    'onb2_body':
        'Brez feedov. Brez algoritmov. Brez klepetalnic. Le signal bližine in ena odločitev: val ali naprej.',
    'onb3_title': 'TVOJE MESTO, V ŽIVO.',
    'onb3_body':
        'Poglej, kdo odkriva v bližini v realnem času. Več kot je Tremble uporabnikov okoli tebe, močnejši je signal.',
    'onb4_title': 'TVOJA PRAVILA.',
    'onb4_body':
        'Odpravi ujemanje ali blokiraj kadarkoli. Enosmerno — nihče ne ve, da si poslal val, dokler ni vzajemno.',
    ```

    **GERMAN (de) — lines ~942–950:**
    Note: German block only has onb1–onb3 (no onb4). Add onb4 after onb3_body:
    ```dart
    'onb1_title': 'ES LÄUFT, WÄHREND DU LEBST.',
    'onb1_body':
        'Tremble arbeitet im Hintergrund. Wenn jemand Passendes physisch in deiner Nähe ist, bekommst du ein Signal. Kein Scrollen nötig.',
    'onb2_title': 'NULL SWIPES. EINE WELLE.',
    'onb2_body':
        'Keine Feeds. Keine Algorithmen. Keine endlosen Chatrooms. Nur ein Näherungssignal und eine Entscheidung: Welle senden oder weiter.',
    'onb3_title': 'DEINE STADT, LIVE.',
    'onb3_body':
        'Sieh, wer gerade in der Nähe entdeckt wird. Je mehr Tremble-Nutzer um dich herum, desto stärker das Signal.',
    'onb4_title': 'DEINE REGELN.',
    'onb4_body':
        'Entmatch oder blockiere jederzeit. Unidirektional — niemand weiß, dass du eine Welle gesendet hast, bis es gegenseitig ist.',
    ```

    **ITALIAN (it) — lines ~1108–1116:**
    Note: Italian block has onb1–onb3. Add onb4 after onb3_body:
    ```dart
    'onb1_title': 'FUNZIONA MENTRE VIVI.',
    'onb1_body':
        'Tremble lavora in background. Quando qualcuno compatibile è fisicamente vicino a te, ricevi un segnale. Niente scroll.',
    'onb2_title': 'ZERO SWIPE. UN\'ONDA.',
    'onb2_body':
        'Nessun feed. Nessun algoritmo. Nessuna chat infinita. Solo un segnale di prossimità e una scelta: onda o vai avanti.',
    'onb3_title': 'LA TUA CITTÀ, IN DIRETTA.',
    'onb3_body':
        'Vedi chi sta scoprendo nelle vicinanze in tempo reale. Più utenti Tremble ci sono intorno a te, più forte è il segnale.',
    'onb4_title': 'LE TUE REGOLE.',
    'onb4_body':
        'Rimuovi il match o blocca in qualsiasi momento. Unidirezionale — nessuno sa che hai inviato un\'onda finché non è reciproca.',
    ```

    **FRENCH (fr) — lines ~1251–1259:**
    Note: French block has onb1–onb3. Add onb4 after onb3_body:
    ```dart
    'onb1_title': 'ÇA TOURNE PENDANT QUE TU VIS.',
    'onb1_body':
        'Tremble fonctionne en arrière-plan. Quand quelqu\'un de compatible est physiquement proche de toi, tu reçois un signal. Pas de scroll.',
    'onb2_title': 'ZÉRO SWIPE. UNE VAGUE.',
    'onb2_body':
        'Pas de fils d\'actu. Pas d\'algorithmes. Pas de salles de chat sans fin. Juste un signal de proximité et un choix : vague ou passe.',
    'onb3_title': 'TA VILLE, EN DIRECT.',
    'onb3_body':
        'Vois qui découvre à proximité en temps réel. Plus il y a d\'utilisateurs Tremble autour de toi, plus le signal est fort.',
    'onb4_title': 'TES RÈGLES.',
    'onb4_body':
        'Supprime le match ou bloque à tout moment. Unidirectionnel — personne ne sait que tu as envoyé une vague jusqu\'à ce que ce soit mutuel.',
    ```

    **CROATIAN (hr) — lines ~1389–1397:**
    Note: Croatian block has onb1–onb3. Add onb4 after onb3_body:
    ```dart
    'onb1_title': 'RADI DOK TI ŽIVIŠ.',
    'onb1_body':
        'Tremble radi u pozadini. Kada je netko kompatibilan fizički blizu tebe, dobivate signal. Bez skrolanja.',
    'onb2_title': 'NULA PREVLAČENJA. JEDAN VAL.',
    'onb2_body':
        'Bez feedova. Bez algoritama. Bez beskonačnih chatova. Samo signal blizine i jedna odluka: val ili nastavi.',
    'onb3_title': 'TVOJ GRAD, UŽIVO.',
    'onb3_body':
        'Vidi tko otkriva u blizini u stvarnom vremenu. Što više Tremble korisnika oko tebe, to je signal jači.',
    'onb4_title': 'TVOJA PRAVILA.',
    'onb4_body':
        'Poništi podudaranje ili blokiraj u bilo kojem trenutku. Jednosmjerno — nitko ne zna da si poslao val dok nije uzajamno.',
    ```

    **SERBIAN (sr) — lines ~1524–1532:**
    Note: Serbian block has onb1–onb3. Add onb4 after onb3_body:
    ```dart
    'onb1_title': 'RADI DOK ŽIVIŠ.',
    'onb1_body':
        'Tremble radi u pozadini. Kada je neko kompatibilan fizički blizu tebe, dobijate signal. Bez skrolanja.',
    'onb2_title': 'NULA PREVLAČENJA. JEDAN VAL.',
    'onb2_body':
        'Bez feedova. Bez algoritama. Bez beskonačnih ćaskanja. Samo signal blizine i jedna odluka: val ili nastavi.',
    'onb3_title': 'TVOJ GRAD, UŽIVO.',
    'onb3_body':
        'Vidi ko otkriva u blizini u realnom vremenu. Što više Tremble korisnika oko tebe, to je signal jači.',
    'onb4_title': 'TVOJA PRAVILA.',
    'onb4_body':
        'Poništi podudaranje ili blokiraj u bilo kom trenutku. Jednosmerno — niko ne zna da si poslao val dok nije uzajamno.',
    ```

    **HUNGARIAN (hu) — lines ~1658–1666:**
    Note: Hungarian block has onb1–onb3. Add onb4 after onb3_body:
    ```dart
    'onb1_title': 'AKKOR IS MŰKÖDIK, AMIKOR ÉLSZ.',
    'onb1_body':
        'A Tremble a háttérben fut. Ha valaki kompatibilis személy fizikailag közel van hozzád, jelet kapsz. Nincs görgetés.',
    'onb2_title': 'NULLA SWIPE. EGY HULLÁM.',
    'onb2_body':
        'Nincs hírfolyam. Nincs algoritmus. Nincs végtelen csevegőterem. Csak egy közelségi jel és egy döntés: hullám vagy tovább.',
    'onb3_title': 'A VÁROSOD, ÉLŐ.',
    'onb3_body':
        'Nézd meg, ki fedez fel a közelben valós időben. Minél több Tremble-felhasználó van körülötted, annál erősebb a jel.',
    'onb4_title': 'A TE SZABÁLYAID.',
    'onb4_body':
        'Szüntesd meg az egyezést vagy blokkold bármikor. Egyirányú — senki sem tudja, hogy hullámot küldtél, amíg nem kölcsönös.',
    ```

    For German, Italian, French, Croatian, Serbian, and Hungarian where onb4 doesn't exist yet: insert the new onb4_title and onb4_body entries immediately before the closing `},` of that language's map, ensuring correct Dart syntax (trailing comma on last entry, no trailing comma violation at map end).
  </action>
  <verify>
    <automated>grep -n "onb1_body\|onb2_body\|onb3_title\|onb3_body\|onb4_title\|onb4_body" lib/src/core/translations.dart | wc -l</automated>
  </verify>
  <done>
    The grep count returns at least 48 (6 keys × 8 languages = 48 occurrences minimum).
    Each language block contains the new brand copy (not the old "Try out one of our premium features" or "A new way to meet people" phrasing).
    Verification: `grep "Try out one\|A new way to meet\|automatically give you a notification" lib/src/core/translations.dart` returns nothing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename greeting_sent to wave_sent in all 8 language blocks</name>
  <files>lib/src/core/translations.dart</files>
  <read_first>
    - lib/src/core/translations.dart (specifically the greeting_sent entries — confirmed at lines 236, 559, 856, 1042, 1184, 1323, 1458, 1593)
  </read_first>
  <action>
    Before making changes, verify no call sites exist outside translations.dart:
    ```bash
    grep -rn "greeting_sent" lib/ --include="*.dart" | grep -v "translations.dart"
    ```
    Expected: no output. If output exists, also update those call sites to use `wave_sent`.

    In translations.dart, rename `greeting_sent` to `wave_sent` AND update the value to remove the `{name}` variable (privacy improvement) in all 8 language blocks:

    **English (line ~236):**
    ```dart
    'wave_sent': 'Wave sent.',
    ```

    **Slovenian (line ~559):**
    ```dart
    'wave_sent': 'Val poslan.',
    ```

    **German (line ~856):**
    ```dart
    'wave_sent': 'Welle gesendet.',
    ```

    **Italian (line ~1042):**
    ```dart
    'wave_sent': 'Onda inviata.',
    ```

    **French (line ~1184):**
    ```dart
    'wave_sent': 'Vague envoyée.',
    ```

    **Croatian (line ~1323):**
    ```dart
    'wave_sent': 'Val poslan.',
    ```

    **Serbian (line ~1458):**
    ```dart
    'wave_sent': 'Val poslat.',
    ```

    **Hungarian (line ~1593):**
    ```dart
    'wave_sent': 'Hullám elküldve.',
    ```

    Each replacement is a simple key rename + value rewrite. The `{name}` interpolation variable is intentionally removed — the toast no longer reveals who you waved at (privacy).
  </action>
  <verify>
    <automated>grep -rn "greeting_sent" lib/ --include="*.dart"</automated>
  </verify>
  <done>
    The grep above returns zero results — greeting_sent no longer exists anywhere in lib/.
    Confirm: `grep -n "wave_sent" lib/src/core/translations.dart | wc -l` returns 8 (one per language).
  </done>
</task>

<task type="auto">
  <name>Task 3: Validate translations.dart compiles clean</name>
  <files></files>
  <read_first>
    - lib/src/core/translations.dart (scan for any obvious Dart syntax issues: unclosed maps, missing commas, unescaped single quotes in strings)
  </read_first>
  <action>
    Run flutter analyze targeted at translations.dart and any file that imports it:

    ```bash
    flutter analyze lib/src/core/translations.dart
    ```

    Common issues to check manually before running analyze:
    1. French and Italian strings with apostrophes — these must be escaped: `'l\'ananas'` not `'l'ananas'`
    2. Trailing comma after last entry in each language map — Dart requires this for proper formatting
    3. Each language map must end with `},` (comma after closing brace, except the very last entry in the outer map)

    If errors found, fix them in translations.dart. Common fix patterns:
    - `Unterminated string literal` → find unescaped `'` inside a `'...'` string and add `\`
    - `Expected ','` → add missing trailing comma after map entry

    Run the full project analyze after fixing:
    ```bash
    flutter analyze
    ```

    Report any warnings that existed BEFORE this plan (pre-existing) vs new ones introduced by edits.
  </action>
  <verify>
    <automated>flutter analyze lib/src/core/translations.dart 2>&1 | grep -c "error:" || echo "0 errors"</automated>
  </verify>
  <done>flutter analyze lib/src/core/translations.dart returns 0 errors. The full `flutter analyze` run produces no new errors compared to pre-plan baseline.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep "Try out one\|A new way to meet\|automatically give you a notification\|are ruining our mental health" lib/src/core/translations.dart` returns nothing
2. `grep -rn "greeting_sent" lib/` returns nothing
3. `grep -c "wave_sent" lib/src/core/translations.dart` returns 8
4. `flutter analyze lib/src/core/translations.dart` is clean
5. Each language block has onb1_title, onb1_body, onb2_title, onb2_body, onb3_title, onb3_body, onb4_title, onb4_body keys
</verification>

<success_criteria>
- Old verbose onboarding copy is replaced with brand voice copy in all 8 languages
- greeting_sent key is gone; wave_sent key exists in all 8 language blocks with privacy-safe value (no {name})
- onb4 keys exist in all 8 languages (was missing from DE, IT, FR, HR, SR, HU)
- translations.dart compiles with zero errors
- BRAND-03 satisfied: onboarding copy is short, direct, confident
- BRAND-04 satisfied: wave_sent key uses correct terminology
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-alignment-wave-mechanic/06-02-SUMMARY.md`
</output>
