---
phase: 06-brand-alignment-wave-mechanic
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/Flutter/Release.xcconfig
autonomous: false
requirements: [BRAND-05]

user_setup:
  - service: google-maps
    why: "Google Maps API key must be wired to both platforms for the map screen to function"
    env_vars:
      - name: MAPS_API_KEY
        source: "Google Cloud Console → APIs & Services → Credentials → your existing Maps SDK key (project: tremble-dev)"
    dashboard_config:
      - task: "Verify the API key has Maps SDK for iOS and Maps SDK for Android enabled"
        location: "Google Cloud Console → APIs & Services → Enabled APIs"
      - task: "Add MAPS_API_KEY as a GitHub Actions repository secret for CI/CD builds"
        location: "GitHub → repository → Settings → Secrets and variables → Actions → New repository secret"

must_haves:
  truths:
    - "ios/Flutter/Release.xcconfig contains MAPS_API_KEY = <real key value>"
    - "Android local.properties already has the real key (confirmed pre-existing)"
    - "iOS Debug.xcconfig already has the real key (confirmed pre-existing)"
    - "The real key value matches across all three platform files"
  artifacts:
    - path: "ios/Flutter/Release.xcconfig"
      provides: "Maps API key for TestFlight and production iOS builds"
      contains: "MAPS_API_KEY ="
  key_links:
    - from: "ios/Flutter/Release.xcconfig"
      to: "ios/Runner/Info.plist"
      via: "MAPS_API_KEY variable referenced by Info.plist as $(MAPS_API_KEY)"
      pattern: "MAPS_API_KEY"
---

<objective>
Wire the Google Maps API key into ios/Flutter/Release.xcconfig (the only platform file missing it), and create a human checkpoint for Aleksandar to verify the key is correct and add it as a GitHub Actions secret.

Purpose: BRAND-05 compliance — Maps API key wired on both platforms for all build variants (Debug + Release + CI).
Output: Release.xcconfig contains MAPS_API_KEY; Aleksandar has verified and added the CI secret.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-brand-alignment-wave-mechanic/RESEARCH.md
@ios/Flutter/Debug.xcconfig
@ios/Flutter/Release.xcconfig
@android/local.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAPS_API_KEY to iOS Release.xcconfig</name>
  <files>ios/Flutter/Release.xcconfig</files>
  <read_first>
    - ios/Flutter/Release.xcconfig (read current content before editing — it currently only has two #include lines)
    - ios/Flutter/Debug.xcconfig (read this to see the exact MAPS_API_KEY line format to replicate)
    - android/local.properties (read to confirm the real key value that is already in place)
  </read_first>
  <action>
    The Debug.xcconfig already contains:
    ```
    // Google Maps API key — get from Google Cloud Console > Credentials
    // Restrict to: iOS app (bundle ID)
    MAPS_API_KEY = REVOKED_KEY_REMOVED
    ```

    The Release.xcconfig currently only has:
    ```
    #include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
    #include "Generated.xcconfig"
    ```

    Add the Maps API key to Release.xcconfig after the existing #include lines:
    ```
    #include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.release.xcconfig"
    #include "Generated.xcconfig"
    // Google Maps API key — for TestFlight and production builds
    // Restrict to: iOS app (bundle ID: tremble.dating.app)
    MAPS_API_KEY = REVOKED_KEY_REMOVED
    ```

    IMPORTANT: Do NOT commit the real key value to git. Verify that ios/Flutter/Release.xcconfig is listed in .gitignore. If it is NOT in .gitignore, add it before committing anything.

    Check .gitignore:
    ```bash
    grep -n "Release.xcconfig\|xcconfig\|Flutter/" .gitignore
    ```

    If Release.xcconfig is not ignored, add `ios/Flutter/Release.xcconfig` to .gitignore before writing the key value.

    Note: The key value `REVOKED_KEY_REMOVED` is already present in android/local.properties and ios/Flutter/Debug.xcconfig — both of which are presumably gitignored or managed via CI secret injection. Apply the same gitignore policy to Release.xcconfig.
  </action>
  <verify>
    <automated>grep "MAPS_API_KEY" ios/Flutter/Release.xcconfig</automated>
  </verify>
  <done>Command above returns the MAPS_API_KEY line with the real key value. Release.xcconfig is confirmed gitignored (grep in .gitignore confirms it).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Claude has added MAPS_API_KEY to ios/Flutter/Release.xcconfig.
    Android local.properties and iOS Debug.xcconfig already had the real key.
    All three platform files now contain the Maps API key.
  </what-built>
  <how-to-verify>
    1. Verify the key is correct and active:
       - Open Google Cloud Console → APIs & Services → Credentials
       - Confirm the key `REVOKED_KEY_REMOVED` exists and is not restricted in a way that would block dev builds
       - Confirm Maps SDK for iOS and Maps SDK for Android are enabled for this key

    2. Add MAPS_API_KEY as a GitHub Actions secret:
       - Go to: GitHub → Pulse---Dating-app repository → Settings → Secrets and variables → Actions
       - Click "New repository secret"
       - Name: `MAPS_API_KEY`
       - Value: `REVOKED_KEY_REMOVED`
       - Click "Add secret"
       - Note: Also update the GitHub Actions workflow files in `.github/workflows/` to inject this secret into local.properties and Release.xcconfig during CI builds (similar to how other secrets are injected)

    3. Test the map screen on a dev build:
       - Run: `flutter run --flavor dev --dart-define=FLAVOR=dev`
       - Navigate to the map screen
       - Confirm the map tiles load (not a grey screen with "For development purposes only" watermark)
  </how-to-verify>
  <resume-signal>Type "verified" when the Maps API key is confirmed active and the GitHub secret has been added. Describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep "MAPS_API_KEY" ios/Flutter/Release.xcconfig` returns the key line
2. `grep "MAPS_API_KEY" ios/Flutter/Debug.xcconfig` returns the key line
3. `grep "MAPS_API_KEY" android/local.properties` returns the key line
4. Map screen renders with tiles on a dev build (verified by human)
5. MAPS_API_KEY exists as a GitHub Actions secret (verified by human)
</verification>

<success_criteria>
- ios/Flutter/Release.xcconfig contains MAPS_API_KEY (enables TestFlight builds to work)
- All three platform config files have the same real Maps API key value
- GitHub Actions secret added for CI builds
- Map screen verified rendering on device/emulator
- BRAND-05 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-alignment-wave-mechanic/06-03-SUMMARY.md`
</output>
